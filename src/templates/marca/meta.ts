import type { MetadadosTemplate } from "../tipos";

export const metaMarca: MetadadosTemplate = {
  id: "marca",
  nome: "Marca",
  descricao: "Logo <MF/> centralizado com glow — avatar, capa, watermark",
  camposUsados: ["tag", "subtitulo", "contato"],
};
